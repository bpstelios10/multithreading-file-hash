management:
  server:
    port: 8081
  endpoints:
    web:
      base-path: /file-hashes/private
      exposure:
        include: prometheus,configprops,env,health,heapdump
      path-mapping:
        prometheus: metrics
  endpoint:
    health:
      probes:
        enabled: true
    heapdump:
      enabled: true

server:
  servlet:
    context-path: /file-hashes
  tomcat:
    accept-count: 120
    mbeanregistry:
      enabled: true
    threads:
      max: 500
